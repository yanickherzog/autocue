# macOS
.DS_Store

# Xcode
*.xcodeproj/*
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
*.xcworkspace/xcuserdata/
xcuserdata/
DerivedData/
*.hmap
*.ipa
*.dSYM
*.dSYM.zip

# Swift Package Manager
.build/
Packages/*/.build/
.swiftpm/

# Claude Code local (personal, machine-specific — not shared project config)
.claude/settings.local.json

# Real-audio test fixtures (ROADMAP.md D8/T8.3) — potentially client-confidential
# source material; this repo is public, so these never get committed, including
# the generated manifest that lives alongside them. See docs/DECISIONS.md.
/Audio_Analysis_Test/
