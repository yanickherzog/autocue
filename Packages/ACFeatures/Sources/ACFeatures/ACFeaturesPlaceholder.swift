import Foundation

/// Scaffolding proving this package builds and links inside AutoCue.xcworkspace
/// — not real feature code. Superseded starting at ROADMAP.md D6 (the real
/// per-window `AppState`, ProjectLibrary feature).
public enum ACFeaturesPlaceholder {
    public static let isScaffolded = true
}
