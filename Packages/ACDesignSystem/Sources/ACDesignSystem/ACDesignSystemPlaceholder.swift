import SwiftUI

/// Scaffolding proving this package builds and links inside AutoCue.xcworkspace
/// — not real feature code. Superseded starting at ROADMAP.md D5 (design tokens,
/// EmptyStateView, ProgressBanner, …).
public enum ACDesignSystemPlaceholder {
    public static let isScaffolded = true
}
