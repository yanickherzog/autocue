import Foundation

/// Scaffolding proving this package builds and links inside AutoCue.xcworkspace
/// — not real feature code. Superseded starting at ROADMAP.md D8 (WAV streaming
/// reader, RIFF/BWF chunk parser, silence/level analysis, waveform peaks).
public enum ACAudioKitPlaceholder {
    public static let isScaffolded = true
}
