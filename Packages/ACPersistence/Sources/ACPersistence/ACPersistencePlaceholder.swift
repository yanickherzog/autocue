import Foundation

/// Scaffolding proving this package builds and links inside AutoCue.xcworkspace
/// — not real feature code. Superseded starting at ROADMAP.md D4 (SwiftData
/// schema, mappers, ProjectRepositoryImpl).
public enum ACPersistencePlaceholder {
    public static let isScaffolded = true
}
