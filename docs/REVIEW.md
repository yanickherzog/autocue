# AutoCue — Review Log

An append-only log, one entry per completed `ROADMAP.md` Deliverable (occasionally per significant Task, for a large Deliverable). This is where architecture/code-quality observations, technical debt, and refactoring suggestions actually get recorded — not left in a closed Claude Code session's memory, not scattered across commit messages nobody re-reads. `CONTRIBUTING.md` §1 requires an entry here as part of finishing a Deliverable; `docs/DefinitionOfDone.md` §6 makes it part of the Definition of Done, not optional polish.

**Rules for this file:**
- **Append, don't edit history.** If a later Deliverable reveals an earlier observation was wrong, add a new entry noting that — don't go back and rewrite the old one. The log's value is in showing what was actually known/believed at each point, including when that turned out to be incomplete.
- **Newest entry last**, so reading top-to-bottom follows the project's actual timeline.
- **Every entry uses the template below, all five fields** — an entry that skips "Technical Debt" because "there wasn't any" should say that explicitly, not omit the heading.

## Template

```markdown
## YYYY-MM-DD — Deliverable DN — <short title>

**Architecture observations:** Did the actual implementation match what CLAUDE.md/ROADMAP.md described going in? Any boundary that was harder to hold than expected, or easier?

**Code quality observations:** Anything about the resulting code worth flagging — good or bad — that isn't already obvious from reading it (a pattern worth repeating elsewhere, a shortcut taken under time pressure, a test that turned out to be more valuable than expected).

**Technical debt:** Anything deliberately deferred, any known gap, any place a future Deliverable will need to revisit this one's work. Reference the specific file/type, not just a vague concern.

**Refactoring suggestions:** Concrete, not vague — "rule of three" candidates (CONTRIBUTING.md §3) that are now two-of-three and worth watching, not "this could probably be cleaner."

**Follow-ups filed:** Any `// TODO(DN.T)` comments added, any `docs/DECISIONS.md` entries this Deliverable produced.
```

---

## 2026-08-08 — Pre-Implementation Architecture Review (Batches 1–4)

Not a `ROADMAP.md` Deliverable in the normal sense — this entry documents the four-batch review-and-correction pass performed on `CLAUDE.md`/`SPEC.md`/`ROADMAP.md` *before* Deliverable D1 (or, at the time, "Milestone 1") began, seeded here so the log has real history from day one rather than starting empty at the first feature Deliverable.

**Architecture observations:** The original architecture documents (single initial commit) were internally consistent enough to look complete on a first read, but contained several real contradictions that would only have surfaced mid-implementation: `CLAUDE.md` told Views both to never touch `@Query`/SwiftData *and* to use `@Query` for list views, in two different sections; the tech-stack table claimed "compiler-enforced module boundaries" for a `Features/` folder that lived inside the app target, where nothing actually enforced that; `AsyncStream` and Foundation's `Progress` were referenced as if interchangeable despite having incompatible concurrency models. None of these would have been caught by "does it compile" — they're the kind of thing that only shows up once two different sessions, months apart, each read a different half of a contradiction and built against it.

**Code quality observations:** Two small pieces of real, tested code were built ahead of schedule specifically to de-risk locked-in architectural bets rather than leave them as unverified assumptions: `Packages/ACCore` (SMPTE drop-frame `Timecode` arithmetic, 18 tests including a 3-hour-equivalent round-trip sweep) and `Packages/ACExport` (a `libxlsxwriter` feasibility spike, verified under real kernel-enforced App Sandbox via an ad-hoc-signed `.app` bundle, not just source-code review). Both are minimal scaffolds, not the real feature work — see the `Package.swift` header comment in each for the explicit "don't mistake this for Deliverable completion" note.

**Technical debt:** `ROADMAP.md`'s pre-restructuring milestones (M13–M25, M31, in the old numbering) still referenced the pre-`ACFeatures`-package `Features/...` path instead of `Packages/ACFeatures/Sources/ACFeatures/...` at the time of this entry — a known, tracked gap, not silently carried forward. The CI workflow (`.github/workflows/ci.yml`) added in this same pass has an unverified assumption: the pinned Xcode version (`15.4`) for GitHub-hosted `macos-14` runners was written without access to a live runner to confirm against — flagged in the workflow file itself as something to confirm on first real run.

**Refactoring suggestions:** None yet — no feature code exists to refactor. The two spike packages (`ACCore`, `ACExport`) will need to be merged into the real Deliverable D1–D3 / D11 package structure rather than treated as separate, permanent scaffolds; that merge is Deliverable D1's job, not a follow-up refactor after the fact.

**Follow-ups filed:** `docs/DECISIONS.md` — full set of entries covering the `MediaDuration` rename, `ACFeatures` package split, drop-frame timecode, `libxlsxwriter` validation, progress/cancellation contract, waveform architecture, deployment target, Views/`@Query` resolution, module dependency graph, navigation model, and document/window model (including its single-window-then-multi-window reversal). See that file directly rather than duplicating its content here.

---

## 2026-08-08 — CI's first real run (not a Deliverable — a same-day follow-up to the entry above)

**Architecture observations:** The CI Xcode-pin risk flagged in the entry above as "unverified against a live runner" was real, not theoretical caution — the very first push after CI was added failed both `swift test` matrix jobs with an identical, unambiguous error (`Package.swift` declared `swift-tools-version: 6.1`, the pinned Xcode 15.4's Swift toolchain is 5.10). Diagnosed directly from `gh run view --log-failed`, not guessed at from symptoms.

**Code quality observations:** N/A — fix was a one-line manifest change in each `Package.swift` (`6.1` → `5.10`), not a code change.

**Technical debt:** None added. If anything, this closes debt: `swift-tools-version` in both packages now reflects an actually-considered floor (matching `CLAUDE.md`'s documented minimum Xcode) instead of whatever the authoring sandbox happened to have installed.

**Refactoring suggestions:** None.

**Follow-ups filed:** `docs/DECISIONS.md` gets a new entry for this fix. `SPEC.md` §6's "CI... hasn't been confirmed against a live runner" gap is updated to reflect that it has now been confirmed, and genuinely failed once before the fix.

---

## 2026-08-08 — CI's second real run (same-day follow-up — the first fix was incomplete)

**Architecture observations:** The entry above closed too early. The `swift-tools-version` fix was pushed and *believed* fixed without actually watching that push's CI result to completion first — a real process lapse worth naming plainly, not glossing over. When actually watched, CI failed again, with a different error: `.swiftformat` separately declared `--swiftversion 6.1` (the same "copied from the local authoring environment" mistake as the tools-version one, just in a second config file), which made SwiftFormat add trailing commas to function-call argument lists — syntax the CI-pinned Xcode 15.4's actual Swift 5.10 compiler cannot parse at all, confirmed by a direct local test compiling a minimal snippet under `-swift-version 5` with the full Xcode toolchain (it succeeded locally — Swift 6.1.2 supports the syntax even in that compatibility mode — which is exactly why local success didn't predict CI's result; this is a compiler-*version* gap, not a language-*mode* gap).

**Code quality observations:** The fix (`.swiftformat`'s `--swiftversion` lowered to `5.10`, matching `swift-tools-version`) is a one-line config change plus a `swiftformat .` re-run — SwiftFormat then correctly stopped producing the problematic syntax on its own, no manual per-line editing needed.

**Technical debt:** None added by the fix itself. The debt this surfaces is process debt: "verified locally" was asserted in the prior entry (and, briefly, in a `docs/DECISIONS.md` entry since corrected) before the corresponding CI run had actually been watched to completion — a gap between "I ran a command that should confirm this" and "I confirmed it," worth remembering on any future infra change in this repo, not just this one.

**Refactoring suggestions:** None.

**Follow-ups filed:** `docs/DECISIONS.md` gets a new entry for this second fix, plus a correction appended to the previous entry's over-claimed "confirmed against the actual failing CI run" line. `SPEC.md` §6 updated to describe both failures and to stop claiming "closed" until a subsequent run is actually observed green.

---

## 2026-08-08 — Deliverable D1 — Workspace, CI, Lint & Core Value Types

**Architecture observations:** Matched `CLAUDE.md`/`ROADMAP.md` closely, with one real gap found and fixed along the way: `ROADMAP.md` T1.1 listed `AppState.swift` as an App-target placeholder file, which directly contradicts `CLAUDE.md`'s Folder/Package Structure section (`AppState` lives in `ACFeatures`, built for real at D6). Resolved in favor of `CLAUDE.md` — `ACFeatures` got a generic placeholder instead, `ROADMAP.md` corrected in the same change. Separately, `SPEC.md` §4.5's literal `Party` definition (`case person(Person.ID)`) looked like a real ordering conflict against `Person`/`Label` not existing until D2, but resolved cleanly without touching the Deliverable split: `Person.ID`/`Label.ID` both resolve to `UUID`, so `Party` built with `UUID`-typed cases today is byte-for-byte what it'd be once D2 lands. The `ACCore`/`ACExport` spike-package merge was a non-event — both already matched the target package shape exactly; only their "not the real thing yet" header comments needed removing.

**Code quality observations:** Hand-authoring `AutoCue.xcodeproj/project.pbxproj` (rather than a generator like XcodeGen) worked, but was the highest-effort, highest-attention part of this Deliverable by a wide margin — every object (`PBXFileReference`, `PBXBuildFile`, `XCSwiftPackageProductDependency`, etc.) needed a hand-tracked unique ID and correct cross-references. `plutil -lint` catches plist syntax errors but not semantic ones (wrong `isa`, dangling ID reference, missing build-phase entry) — the only real confirmation was a genuine `xcodebuild build`/`test` run. Worth noting for whoever hand-edits this file next: treat it the same way, don't assume `plutil -lint` passing means it's correct.

**Technical debt:** None deliberately deferred within D1's own scope. The App target is intentionally minimal (no entitlements, no `Assets.xcassets`, placeholder `AutoCueApp.swift`/`DependencyContainer.swift`) — that's D6/T6.1's job, not debt.

**Refactoring suggestions:** None yet — five of the seven packages are still pure scaffolding (one placeholder type + one placeholder test each), nothing to refactor until real Deliverable work lands in them.

**Follow-ups filed:** `docs/DECISIONS.md` gets two new entries: `Party`'s `UUID`-typed-cases resolution, and the hand-authored-`pbxproj` tooling choice (including a real, since-resolved local-environment blocker — a broken Xcode 26.3 install missing `DVTDownloads.framework`, fixed via `sudo xcodebuild -runFirstLaunch` run by the user in their own terminal, not from an unattended session). `ROADMAP.md` D1 marked complete, with its T1.1 `AppState.swift` cross-reference corrected in the same change. CI run against the actual Xcode-15.4-pinned runner is still pending at the time of this entry — push/watch happens after user review, per this project's own established practice of treating a real CI run, not local success, as the final bar.

---

## 2026-08-08 — Deliverable D2 — Domain Identity, Setup & Cue Models

**Architecture observations:** Two real gaps surfaced during planning, before any T2.x code was written — both fixed in this Deliverable rather than carried forward silently. First: D1 had claimed `MediaDuration` was implemented ahead of schedule alongside `Timecode`/`TimecodeFrameRate`; checking the actual source tree (not trusting the prior claim) found it was never merged — no `MediaDuration.swift` existed anywhere. Since `Setup.productionRuntime`/`totalMusicRuntime` and `Cue.duration` all depend on it directly, this was built first, as corrective work, with `ROADMAP.md`'s D1 section corrected to admit the gap plainly rather than rewritten to look clean. Second: `SPEC.md` specified `CueRightHolder.arrangementAuthorizationAttached` as required "iff... the original work is still copyright-protected," but no field anywhere in the schema (`Cue` or `CueRightHolder`) actually carried that fact — the condition referenced data that didn't exist. Resolved by adding `Cue.isArrangementOfProtectedOriginal: Bool` (default `false`), as a property of the work rather than of any one right-holder row, with `SPEC.md` §4.3/§4.4/§4.6 and `CLAUDE.md`'s Data Model summary updated in the same change per rule 9. Beyond those two, D2 matched `CLAUDE.md`/`ROADMAP.md` closely: `Party`'s D1-era `UUID`-typed cases resolved to `Person.ID`/`Label.ID` with genuinely zero rework, confirming the forward-compatibility claim from D1's `docs/DECISIONS.md` entry held exactly as predicted. The delete-guard (§4.12), `PartyResolver` (§4.13), and `RecalculateTotalMusicRuntimeUseCase` (§4.14) were all correctly left for D3/D10 rather than built ahead of schedule, even though `PartyResolver` in particular was technically buildable the moment `Person`/`Label` existed — held off specifically to respect `ROADMAP.md`'s Task boundaries per `CONTRIBUTING.md` §1, not because of a technical blocker.

**Code quality observations:** A real correctness trap surfaced while writing `ValidateCueRightHolderSharesUseCase`'s tests: a `Decimal` float literal (e.g. `33.33` written directly as a `Decimal`-typed argument) is *not* exact — Swift converts it via `Double` first, so `CueRightHolder(performanceBroadcastShare: 33.33, ...).performanceBroadcastShare` printed as `33.32999999999999488` in a failing test, not `33.33`. This is exactly the binary-rounding problem `Decimal` was chosen specifically to avoid (SPEC.md §4.6), just relocated to how a `Decimal` value gets *constructed* rather than how it's summed. Fixed throughout by constructing exact test fixtures via `Decimal(string:)` (wrapped in `XCTUnwrap`, never force-unwrapped, per `CONTRIBUTING.md` §9) instead of float literals. Worth remembering for any future Deliverable that parses user-entered percentage shares from text fields (D7/D10): the input must go through `Decimal(string:)`-style exact parsing, not a `Double`-typed text-field binding coerced to `Decimal` after the fact, or this Deliverable's whole reason for choosing `Decimal` is silently defeated at the UI boundary. `ValidateCueRightHolderSharesUseCase` and (once D3 lands) `PartyResolver` now share one real precedent for a Repository-free Use Case: a `public enum` namespace with `static func`, matching the "pure static helper" shape SPEC.md §4.14 already specifies for `RecalculateTotalMusicRuntimeUseCase` — worth following again rather than defaulting to an instantiable `struct` for the next Repository-free Use Case that comes up.

**Technical debt:** `CueRightHolder` has no `id` field — matching SPEC.md §4.4's schema exactly, not an oversight, but a real forward risk worth flagging explicitly rather than only mentioning in passing: D10/T10.2's per-cue right-holder editing UI (add/remove/reorder within `Cue.rightHolders`) will need per-row identity for SwiftUI list diffing, and `CueRightHolder` currently has nothing to provide it. Whether that's solved by finally giving it an `id` field (with a `SPEC.md`/`CLAUDE.md` update at that point, per rule 9) or by some index-based identity scheme is an open question for D10 to resolve, not decided here — don't add an `id` preemptively before D10 actually needs it (rule 7). Separately, not debt but worth naming: `RecalculateTotalMusicRuntimeUseCase` (SPEC.md §4.14) and `PartyResolver` (§4.13) are still unbuilt — correctly scoped to D10 and D3 respectively, not gaps in D2.

**Refactoring suggestions:** None yet. The "small, tightly-coupled enum co-located in its parent model's file" convention (`TimecodeComponents` in `Timecode.swift` from D1; `AdditionalWorksDeclaration` in `Setup.swift`, `CueSource` in `Cue.swift`, `CueRightHolderRole` in `CueRightHolder.swift` from D2) is now used four times — not a "rule of three" duplication candidate (there's no shared logic to extract, just a shared file-organization choice), but worth continuing to apply deliberately rather than reconsidering per type.

**Follow-ups filed:** `docs/DECISIONS.md` gets one new entry: the `MediaDuration` gap and its correction. `ROADMAP.md` D1's Status/T1.4 corrected to admit the gap; D2 and its three Tasks marked complete in the same change. `SPEC.md` §4.3/§4.4/§4.6 and `CLAUDE.md`'s Data Model section updated for `Cue.isArrangementOfProtectedOriginal`. No new `docs/DECISIONS.md` entry was needed for that field itself — closing an undocumented schema gap the first time a Deliverable actually needed it isn't the kind of debatable trade-off that file's own bar calls for, unlike the `MediaDuration` case, which involved correcting a previously-recorded (and wrong) status claim.

---

## 2026-08-08 — Deliverable D3 — Domain Composition & Repository Protocols

**Architecture observations:** Matched `CLAUDE.md`/`ROADMAP.md`/`SPEC.md` closely, with no gaps found during planning this time (unlike D1's `MediaDuration` and D2's `isArrangementOfProtectedOriginal` gaps, both caught before the corresponding Deliverable started). `Project` composes cleanly from D1/D2's types plus the remaining leaf models (`AudioAsset`, `EmbeddedMarker`, `BroadcastWaveMetadata`, `Settings`/`AnalysisSettings`, `WaveformPeaks`/`WaveformPeakBucket`); the three Repository protocols (`ProjectRepository`, `AudioAnalysisRepository`, `ExportRepository`) came out exactly the shape D3's own planning described, including `ExportRepository` staying deliberately thin (no `computeLayout` method — it can't exist until `CueSheetPageLayout` does, at D11/T11.2, so it wasn't stubbed in ahead of that). `DeleteRightHolderUseCase`'s guard scans exactly one caller-supplied `Project`, never across projects — documented directly on the type specifically so a later Deliverable (D6's multi-window shell) doesn't casually violate it by passing every open `Project` "to be safe."

**Code quality observations:** `AudioAsset`'s "never contains raw or downsampled sample data" invariant (SPEC.md §4.10) is enforced by a `Mirror`-based structural test — asserting no such stored property exists at all — rather than only a doc comment asserting intent; a real, if unusual, way to make an architectural invariant fail loudly if anyone ever adds a sample-data field to that type by accident. `InMemoryProjectRepository` was built as an actor supporting multiple *concurrent* `observeAll()` subscribers from the start, each independently receiving every mutation's fresh snapshot — not the minimum needed for D3's own tests, but specifically because D11's planned cross-ViewModel propagation test needs exactly this, and retrofitting multi-subscriber support onto a single-subscriber fake later would have been real rework. `DeleteRightHolderUseCase`'s pure `referenceLocations` scan (no Repository call) is tested mock-free at the `ACCore` level; the repository-touching `deletePerson`/`deleteLabel` orchestration is exercised separately in `ACTestSupportTests` against the real `InMemoryProjectRepository` fake, not a second mock — consistent with `CONTRIBUTING.md` §5's "an `ACCore` test that needs a mock means something's wrong."

A real bug was caught and fixed within this Deliverable, not carried forward: `DeleteRightHolderUseCase`'s internal `replacing(_:people:labels:)` helper carried the original `Project.updatedAt` forward unchanged after a successful deletion. `SPEC.md` §4.1 gives `updatedAt` no narrower scope than "this Project's data changed," and nothing qualifies it to exclude the right-holder directory specifically — fixed, with orchestration tests asserting `updatedAt` advances on a successful delete and stays untouched when a delete is correctly blocked. Separately, a documentation-only correction: `ROADMAP.md`, `CLAUDE.md`, and `SPEC.md` all still described the delete-guard Use Case by an older two-type-or-shared-alternative phrasing from before D3 actually built it as one shared `DeleteRightHolderUseCase` with `deletePerson`/`deleteLabel` methods — six stale references across the three files, including one (`ROADMAP.md` D7's acceptance criteria) that would have pointed a future session at a type that doesn't exist. All six corrected in the same PR rather than left to confuse D7.

**Technical debt:** None deliberately deferred within D3's own scope. `ExportRepository`'s thinness (above) is a real, tracked gap, not an oversight — it's D11/T11.2's job to fill in once `CueSheetPageLayout` exists.

**Refactoring suggestions:** None yet.

**Follow-ups filed:** No new `docs/DECISIONS.md` entry — D3 had a real bug fix (`updatedAt`) and a real doc-consistency correction (the Use Case naming), but neither is the kind of debatable architectural trade-off that file's own bar calls for; both are recorded here and in the PR history instead. `ROADMAP.md`, `CLAUDE.md`, `SPEC.md` all corrected for the `DeleteRightHolderUseCase` naming, in the same change that built it. **This entry itself is a same-day-scale follow-up, not written at the time D3 was merged** — `ROADMAP.md` D3 was also never marked "Status: Complete" the way D1/D2/D4 are, a second gap from the same root cause (this file wasn't updated when D3 actually finished); noted here rather than silently fixed, since closing that specific gap wasn't this entry's ask.

---

## 2026-08-09 — Deliverable D4 — Persistence

**Architecture observations:** The persistence design itself matched `CLAUDE.md`/`ROADMAP.md` closely: `@Model` entities map 1:1 to `ACCore` domain aggregates with all conversion isolated in `Mappers/`; `Party` references stay bare kind+id columns, never promoted to a real `@Relationship`, specifically to avoid reintroducing cascade/nullify behavior behind D3's `DeleteRightHolderUseCase` delete guard's back; `observeAll()`'s live-observation mirrors `InMemoryProjectRepository`'s existing subscriber-dictionary contract exactly, now backed by real SwiftData. `ProjectRepositoryImpl`'s per-`Project.ID` write serialization (`CLAUDE.md`, "Document & Window Model") needed real, non-obvious design: `ModelContext`'s fetch/save calls are synchronous, not `async`, so a naive `actor`-only implementation would not have delivered "different `Project.ID`s don't block each other" — the actual design chains each write onto that ID's previous pending `Task`, with the real SwiftData work dispatched into its own unstructured `Task` per call, off the actor's exclusive-execution path. One deliberate scope deviation from T4.1's literal file list: `SettingsEntity` was not built (see `ROADMAP.md` D4's Status note for why) — `Settings` isn't part of `Project`'s aggregate and has no repository until D14.

The real story of this Deliverable, though, is the investigation, not the schema. Four consecutive code-level fixes for a fully deterministic `ACPersistence` CI crash — a `WaveformPeaksEntity` redesign (`Data` blob → native `[Float]` attributes), dropping duplicate `@Attribute(.unique)` constraints, building a fresh `Schema` per `ModelContainer` instead of sharing one `static let`, and reordering mapper writes to fix a hypothesized Swift exclusivity violation — each passed every local check and each turned out to have **zero effect** on the actual crash. The fourth fix was based on a real crash report pulled from the CI runner (`xctest-*.ips`, via a temporary diagnostic CI step), which still turned out to be a misread. What actually found the cause, at the user's explicit direction, was abandoning code-level guessing entirely: a staged fixture bisection (seven tests, each adding one piece of complexity) found the crash already reproduces at the simplest possible case — one `Cue`, zero `CueRightHolder`s — ruling out every one of the four "fixes" outright, since none of them touch code that case even reaches. An isolated toy `@Model` pair (`ToyParent`/`ToyChild`), with zero connection to any real entity in this codebase but the same relationship shape, reproduced the crash identically on CI and never once locally, under both `swift test` and a from-scratch `xcodebuild test` run against a bare, un-wrapped SPM package. That pointed at the toolchain itself, not the code: per GitHub's `runner-images` docs, `macos-15` offers Xcode up to `26.3` (the exact local version already confirmed clean) versus `macos-14`'s ceiling of `16.2` (with `15.4` — this project's original pin — as default). A config-only `ci.yml` change (zero source files touched, to cleanly isolate the one variable) repinned CI to `macos-15`/Xcode `26.3`. Result: `ACPersistence`'s CI job went from 8 consecutive real-run failures to a clean, fully green pass, unchanged code, confirmed again on the post-merge `main` run.

**Code quality observations:** The per-`Project.ID` write-tail chaining pattern in `ProjectRepositoryImpl` is the one genuinely novel piece of concurrency architecture in this Deliverable, and it earned real scrutiny before being trusted — including an explicit, load-bearing invariant (the read-modify-write of the tail dictionary must contain no `await`) documented directly in the source as a comment, not just asserted in a PR description. The persistence-only `order` field needed on `CueEntity`/`CueRightHolderEntity`/`PersonEntity`/`LabelEntity`/`EmbeddedMarkerEntity` (SwiftData gives no to-many fetch-order guarantee, but `Project`'s synthesized `Equatable` compares arrays order-sensitively) was **not** anticipated going in — it was caught by an actual failing round-trip test (`Person`/`Label` order flipping across runs) before the fix reordering ever reached CI, a real example of a round-trip test earning its keep rather than being pro-forma coverage. Every mapper that assembles a parent + to-many children (`ProjectMapper`, `CueMapper`, `AudioAssetMapper`) now uses an identical "materialize children fully, assign to parent, set back-references afterward" pattern — three real instances of the same shape, each carrying its own doc comment explaining why the ordering was investigated (even though the investigation's conclusion was that this specific ordering wasn't what fixed the crash) — a rule-of-three candidate for extraction into a shared helper if a fourth relationship-with-backreference mapper is ever added, not extracted now since the per-call-site comments carry real, non-obvious context a generic helper would obscure.

**Technical debt:** None deliberately deferred within the persistence schema itself. The one real, load-bearing thing this Deliverable changes for every future session: the project's actual minimum viable Xcode version is now `26.3`, not `15+` — `CLAUDE.md`'s "Deployment Target" section is corrected in this same PR, but any contributor (human or a future Claude Code session) who assumes "Xcode 15.x should work, `CLAUDE.md` said so" before this correction landed would hit a real, reproducible crash the moment they touched a `@Model` relationship, not a hypothetical risk.

**Refactoring suggestions:** The materialize-then-assign-then-backreference mapper pattern (above) — watch for a fourth call site; that's the point at which extraction stops being premature.

**Follow-ups filed:** `docs/DECISIONS.md` gained three real entries from this investigation, two of them carrying their own honest corrections rather than silent edits: the `WaveformPeaksEntity` redesign (corrected once proven not to be the cause), "the real cause of PR #4's crash" — the exclusivity-violation diagnosis (also corrected once proven wrong), and the final "D4's real, confirmed root cause" entry documenting the actual Xcode 15.4 SwiftData defect and the CI fix. `CLAUDE.md`'s "Deployment Target"/"Toolchain implication" section corrected in the same PR. `ROADMAP.md` D4 and its three Tasks marked complete, with the `SettingsEntity` scope deviation noted explicitly rather than silently. Two throwaway diagnostic files (`BisectionTests.swift`, `AAAToyRelationshipTests.swift`) were created during the investigation and both removed once their jobs were done — neither shipped in the merged state.

---

## 2026-08-09 — Deliverable D5 — Design System Foundations

**Architecture observations:** The real work in this Deliverable wasn't the tokens/components themselves — it was discovering, before any code was written, that the visual design AutoCue actually targets (brand palette, Space Grotesk, sharp corners, ghost-text tint, transition timing, default window size) had never been captured anywhere in this repository. `SPEC.md` §3 explicitly scopes UI/visual design out of that document, and `ROADMAP.md` D5's own Acceptance Criteria — "every color/font token uses semantic, dark-mode-safe definitions," "a SwiftUI preview renders a token swatch sheet in both appearances" — turned out to be generic SwiftUI-best-practice boilerplate written before any real brief existed, not a description of what this project actually wants. A direct grep across every doc in the repo (`SPEC.md`, `CLAUDE.md`, `ROADMAP.md`, `CONTRIBUTING.md`, `docs/DECISIONS.md`, `docs/DefinitionOfDone.md`, `docs/REVIEW.md`) for the specific values in the brief (`Space Grotesk`, `Burgundy`, `93032E`, `Carbon Black`, `202020`, `ghost text`) came back with zero matches before implementation began — confirmed rather than assumed, the same discipline this log has applied to every other claim worth checking. Once the real brief was supplied, it directly conflicted with the roadmap's placeholder wording: AutoCue commits to two **fixed** surface styles selected per screen (Setup/Cue Sheet: white background, black text; Review & Export: black background, white text), not colors that adapt to the system Light/Dark Mode setting. Resolved by treating this as a real, debatable architectural call worth its own `docs/DECISIONS.md` entry ("AutoCue does not adapt to system Light/Dark Mode") and correcting `ROADMAP.md` D5's Acceptance Criteria in the same change, rather than silently building against the literal old wording or silently building against the new brief without reconciling the two on the record.

**Code quality observations:** Two real bugs were caught during local verification, before either reached CI — worth recording because both are the kind of mistake that "it compiled" would not have caught. First: `Theme.Typography`'s lazy font-registration trigger called `Bundle.module.url(forResource:withExtension:)` without a `subdirectory` argument. `Package.swift` declares the font resources as `.copy("Resources/Fonts")`, which — confirmed by inspecting the actual built `.bundle` directory structure, not assumed from the API name — copies the whole `Fonts/` folder as a subdirectory *inside* the resource bundle, not flattened to the bundle root. The unqualified lookup would have returned `nil` for every weight, silently, both in the real running app and in Previews; a real font-registration failure that "the code compiles and the previews render sample text" would never have surfaced, since SwiftUI's `Font.custom` silently falls back to a system font on a missing PostScript name rather than crashing. Caught by a real test (`test_bundledFontWeights_areActuallyPresentInTheResourceBundle`) that inspects the resource bundle directly rather than trusting the registration call to have worked — the same "verify the artifact, don't trust the API call" discipline `ACExportTests`' `XLSXFeasibilitySpikeTests` established for `libxlsxwriter`. Second: `Theme.Colors.Surface` and `Theme.Typography.Weight` were originally nested two levels deep (`Theme.Colors.Surface`, `Theme.Typography.Weight`), which `swiftlint lint --strict` flagged as two `nesting`-rule errors — not caught by `swift build`/`swift test`, only by actually running the project's real lint tooling locally before pushing, consistent with `CONTRIBUTING.md` §8's "run it before committing, don't let CI be the first place that notices." Fixed by flattening both to direct `Theme` siblings (`Theme.Surface`, `Theme.FontWeight`) rather than suppressing the rule — the fix was mechanical (a rename plus a `sed` pass across every consuming file) once the actual violation was understood.

**Technical debt:** `Theme.Colors.ghostTextPrimary` (Carbon Black at 40% opacity) is defined for the primary surface only — no reversed-surface ghost-text token exists yet, deliberately, since nothing in `SPEC.md`/`ROADMAP.md` currently puts an input field on a reversed-surface (Review & Export) screen. Flagged explicitly here, per `CLAUDE.md` rule 7, so a future Deliverable that does add one doesn't have to rediscover this reasoning from scratch — add the token then, with a real screen driving its value, not speculatively now. Separately: only Regular/Medium/Bold Space Grotesk weights are vendored (no Light) — a deliberate call, not an oversight (see the conversation record for this Deliverable), on the reasoning that the 40%-opacity ghost-text token already carries all the de-emphasis this project currently needs, and stacking a second de-emphasis signal (weight) on top would risk placeholder text visibly changing weight the instant a user starts typing real (Regular-weight) input.

**Refactoring suggestions:** None yet — `ACDesignSystem` has too little real content so far (one Deliverable's worth) for a genuine rule-of-three candidate to have emerged. Worth watching once D6+ start consuming these tokens: if `SharpButtonStyle`'s two-`Emphasis`-case shape turns out too narrow for a third real button treatment a later screen needs, that's the signal to revisit it, not a hypothetical to design around now.

**Follow-ups filed:** `docs/DECISIONS.md` gained one new entry ("AutoCue does not adapt to system Light/Dark Mode — color tokens are fixed-value"). `ROADMAP.md` D5's Acceptance Criteria corrected in the same PR (#7) that built the Deliverable, with the correction reasoning stated inline rather than the original placeholder wording silently replaced. `CLAUDE.md` gained a new "Visual Language" subsection under Design System — the durable source of truth D6 onward should check against rather than each re-deriving "the accent color" independently. `ROADMAP.md` D5 marked complete in this follow-up change, matching D3's precedent (PR #6) for closing the same two-item gap (Status line, this entry) in a small docs-only PR after the feature PR merged.

---

## 2026-08-10 — Deliverable D6 — Multi-Window App Shell & Project Library

**Architecture observations:** The real work in this Deliverable wasn't the shell itself — `WindowGroup`/`WindowGroup(for: Project.ID.self)`/`Settings` came together exactly as `CLAUDE.md`'s "Document & Window Model" already specified — it was a domain-model gap the "create a named project" flow exposed that no earlier Deliverable had actually exercised: `Setup.producer`/`.directorOrPrincipal`/`.declarant: Party` have no honest "unset" value (`Party` has only `.person`/`.label` cases), so a brand-new `Project` with an empty right-holder directory couldn't construct a valid `Setup` at all. The first instinct — auto-create a placeholder `Person` and point all three fields at it — was caught and rejected before any code was written, specifically because it reproduces the exact "a default value that could silently satisfy a required field" pattern D2's own acceptance criterion already named as unacceptable; the fix instead makes the three fields genuinely `Party?`, checked at export-readiness time (D11) instead of construction time. Every field in `SPEC.md` §4.2 was re-audited individually against the same question before concluding only these three had the problem — worth the exercise, since the first pass at this Deliverable would have shipped the placeholder-`Person` version if not stopped. A second real design question, resolved cleanly: `ProjectLibraryView` (`ACFeatures`) has no way to reach `OpenProjectWindowRegistry`/`NSWindow`/`openWindow` (App-target/AppKit concerns `ACFeatures` can't import), so "open this project" crosses that boundary as a plain `onOpenProject: (Project.ID) -> Void` closure supplied by the App target's own `LibraryWindowView` — the same adapter-at-the-edge pattern `WaveformView`/`CueTableView` already establish, now shown to generalize past just the `ACDesignSystem` boundary to the `ACFeatures`/App-target one too.

**Code quality observations:** `ProjectLibraryViewModel`'s `observationTask: Task<Void, Never>?` needed to be cancelled from `deinit`, but `deinit` is never actor-isolated even on a `@MainActor` class — plain `nonisolated` isn't legal on a mutable stored property, and applying it anyway (following the compiler's own suggested fix literally) produced a confusing indirect error through the `@Observable` macro's synthesized backing storage. Resolved by pairing `@ObservationIgnored` with `nonisolated(unsafe)` — removing the property from Observation's tracked-property rewriting first, so the attribute lands on a genuinely plain stored property instead of a macro-synthesized computed one. Worth remembering for the next `@Observable` ViewModel that needs a similar deinit-cancelled background `Task`: `@ObservationIgnored` first, then `nonisolated(unsafe)`, not `nonisolated` alone. Three small, stateless `ACCore` Use Cases (`ObserveProjectsUseCase`/`CreateProjectUseCase`/`DeleteProjectUseCase`) were added beyond `ROADMAP.md` T6.2's literal file list so `ProjectLibraryViewModel` calls Use Cases only, never `ProjectRepository` directly — consistent with `CONTRIBUTING.md` §6 and the `ObserveProjectsUseCase` name `CLAUDE.md`'s D4 decision entry already anticipated; `CreateProjectUseCase.makeDefaultSetup` is a pure static helper (no Repository call), tested mock-free at the `ACCore` level, with its repository-touching `create(name:)` orchestration tested separately against `ACTestSupport`'s real `InMemoryProjectRepository` — the same pure/orchestration split `DeleteRightHolderUseCase`/`DeleteRightHolderOrchestrationTests` already established at D3.

**Technical debt:** None deliberately deferred within this Deliverable's own scope. Two things intentionally left as placeholders, correctly per D6's own stated scope, not oversights: the three Project-window section tabs (Setup/Cue Sheet/Review & Export) render a plain `EmptyStateView` each, with their real content coming at D7/D9–D10/D11; the `Settings` scene similarly shows a placeholder, real content at D14. `productionYear`'s default of `0` (not a `Calendar`-derived current year) is called out with an explicit in-code comment on `CreateProjectUseCase.makeDefaultSetup`, specifically so a future session doesn't "helpfully" change it — flagged here too so it isn't only discoverable by reading that one file.

**Refactoring suggestions:** None yet. `LibraryWindowView`/`ProjectWindowView` are the first two App-target scene-root Views this project has needed beyond `AutoCueApp.swift` itself — worth watching whether a third scene root (this Deliverable only has two) reveals a shared pattern worth extracting, not decided now.

**Follow-ups filed:** `docs/DECISIONS.md` gained one new entry ("`Setup`'s three `Party` fields become optional: `producer`/`directorOrPrincipal`/`declarant`"), including the full field-by-field audit table. `SPEC.md` §4.2 and §4.6 updated in the same change (rule 9) — the three fields' Required column and a new §4.6 cross-field validation bullet for their export-readiness check. `ROADMAP.md` D6 and its two Tasks marked complete, with the Use Case scope addition and the domain-model correction both stated inline rather than silently absorbed. **Not yet done, flagged here rather than silently claimed:** the user's own interactive click-through of the actual acceptance criteria (create/list/open-and-focus/delete a project by hand) — this session verified the app builds, tests, launches, and runs sandboxed with no violations, but has no GUI-automation tool to drive the manual walkthrough itself; and a real, watched-to-completion CI run on the pushed branch, per this project's own standing practice of treating that as the actual bar, not local verification alone.
